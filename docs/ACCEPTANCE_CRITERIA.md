# Version 1 acceptance criteria

Each criterion maps to requirements in [REQUIREMENTS.md](REQUIREMENTS.md). Evidence must identify build/commit, fixture or test, environment, and result.

| ID | Requirements | Acceptance criterion |
|---|---|---|
| AC-001 | FR-001, UX-001, UX-002 | Given a first-time user and valid defaults, when they complete the main flow, then they can create and reach export preflight for an 8-bar section without external instructions; invalid fields are specific and retained. |
| AC-002 | FR-002, MUS-001 | Given a supported brief, generation returns distinct chord, bass, arp, and lead canonical tracks whose events validate against typed domain schemas. |
| AC-003 | MUS-001, MUS-005 | Golden harmony cases contain valid chord membership, pitch ranges, spacing, inversion, and configured voice-leading bounds; an unsatisfiable configuration returns a structured error. |
| AC-004 | FR-006, NFR-001 | Given identical canonical inputs, generator/engine/profile versions, and seed, repeated runs produce byte-equivalent normalized canonical JSON across supported environments. |
| AC-005 | FR-005 | Given locked chords, arp, and lead, when bass is regenerated, those three components' canonical hashes remain unchanged. |
| AC-006 | FR-006, NFR-002 | A variation stores parent ID, seed, versions, parameters, author/time, and result hash; creating it does not overwrite the parent. |
| AC-007 | MIDI-001, MIDI-003 | MIDI fixtures declare 960 PPQ, valid tempo/time-signature metadata, positive durations, matching note termination, and stable same-tick ordering. |
| AC-008 | MIDI-002 | Round-trip comparison preserves each exported event's pitch, start tick, duration tick, velocity, tempo, PPQ, channel policy, and track name. |
| AC-009 | FR-008, FR-013, MIDI-004 | The export package contains expected component MIDI, canonical JSON, notes, and README; supported FL Studio imports align to 8 bars with correct tempo and track identity. |
| AC-010 | MUS-002 | Bass golden cases receive progression context; strong-beat notes meet the selected root/chord-tone policy and permitted exceptions are explicit in parameters/provenance. |
| AC-011 | MUS-003 | Each arp event is derived from active harmony and satisfies configured rate, range, direction, gate, density, and seed behavior. |
| AC-012 | MUS-004 | Motif fixtures expose motif identity and transformations; configured chord targets, range, leap, repetition, phrase, and resolution constraints are testable. |
| AC-013 | MUS-006 | All generated events start at or after tick 0, have positive duration, and end at or before the section boundary. |
| AC-014 | FR-003, UX-003, A11Y-004 | Transport play/stop/loop and mute/solo stay synchronized within the documented preview tolerance; playback is user-initiated and failures degrade safely. |
| AC-015 | FR-004, UX-003 | Add/delete/drag/resize/velocity/transpose/snap each produce valid state, and undo/redo restores exact prior/next canonical editor revisions. |
| AC-016 | FR-007 | Save/reopen returns the same latest authorized composition state and history; offline/conflict paths preserve unsaved work. |
| AC-017 | AI-001, AI-002, AI-003, SEC-003 | Malformed, out-of-range, injected, or extra-field model outputs fail closed at schema/tool boundaries and cannot mutate canonical state; raw-note output is rejected. |
| AC-018 | FR-009, FR-011, AI-004 | Coach/explanation responses reference computed observations and label subjective advice; reviewers can trace each claim to current structured state. |
| AC-019 | FR-010, AI-002 | A supported intent phrase maps only to allowed, schema-valid parameter changes; the user can inspect scope before deterministic application. |
| AC-020 | FR-007, SEC-001, SEC-004 | Ownership tests prove users can CRUD their rows, cannot discover/access another user's rows, and deletion targets only confirmed owned resources with expected cascades. |
| AC-021 | FR-012 | Supported synth recipes validate against the profile vocabulary, identify assumptions, and never invent an unsupported control as a factual parameter. |
| AC-022 | SEC-001, SEC-002, SEC-003, SEC-004 | Automated RLS/API negative tests deny anonymous/cross-user/privileged-client access, secrets are absent from artifacts, and destructive/malformed requests fail safely. |
| AC-023 | SEC-005 | Cost-bearing endpoints enforce documented identity, request size, concurrency, rate, and timeout budgets with retry-safe errors. |
| AC-024 | AI-005, NFR-004 | Correlated logs contain request/generation IDs, versions, seed, latency, validation outcome and allowed cost metadata while redaction tests exclude secrets/tokens/unnecessary content. |
| AC-025 | UX-002 | Empty, loading, invalid, generation failure, save conflict, offline, and export failure states each provide a clear next action and retain recoverable work. |
| AC-026 | UX-004 | Defined desktop widths support full authoring; defined small-screen widths support inspection/audition/basic configuration without clipped critical actions. |
| AC-027 | A11Y-001, A11Y-002, A11Y-003, A11Y-004 | Automated WCAG checks plus manual keyboard, focus, zoom, contrast, semantics, screen-reader, target-size, motion, and audio checks pass the release checklist. |
| AC-028 | NFR-003 | Representative deterministic generation meets the release performance budget established and approved in Stage 17, with regression thresholds stored in CI. |
| AC-029 | NFR-005 | Required CI jobs and human evaluation gates in `TESTING_STRATEGY.md` pass on the release commit; no skipped release-blocking test is unexplained. |
| AC-030 | NFR-006 | A fixture from each supported prior canonical schema version migrates deterministically or produces an explicit supported-version error without data loss. |
| AC-031 | NFR-007 | Given a clean checkout and the selected Node/npm policy, `npm ci` and every required validation command complete successfully using the committed lockfile; direct versions and dependencies match the authoritative register. |
| AC-032 | UX-005 | The rendered foundation shell has one clear application identity and heading hierarchy, semantic landmarks, visible keyboard focus, responsive reflow, no baseline automated accessibility violations, working error/not-found recovery, and no control or copy that implies composition features exist. |
| AC-033 | MIDI-001, MUS-007 | Every absolute tick from 0 through 30720 round-trips through the zero-based 8-bar 4/4 position converter; invalid numeric/position/duration inputs fail with stable typed errors; event starts are before 30720 and positive durations may end exactly at, but never after, that boundary. |
| AC-034 | NFR-008 | Repeated serialization of each validated Stage 3A time primitive produces its exact versioned fixed-order JSON fixture without locale, timezone, framework, provider, or ambient-clock input. |
| AC-035 | MUS-001, MUS-008, NFR-009 | All pitch classes 0–11 and MIDI pitches 0–127 construct and serialize deterministically; every MIDI pitch extracts `midiPitch % 12`; invalid numeric inputs fail with stable typed errors; no canonical value or serialized field encodes enharmonic spelling or an octave-label convention. |
| AC-036 | MUS-001, MUS-009, NFR-010 | Representative signed and compound intervals, including negative values, zero, and positive values, construct and serialize deterministically; invalid/fractional/non-finite/unsafe values fail explicitly; negation, comparison, addition, subtraction, and overflow behavior are tested; all 16,384 ordered MIDI-pitch pairs satisfy directed `to - from` distance and antisymmetry. |
| AC-037 | MUS-001, MUS-010, NFR-011 | The six closed scale formulas are exact and immutable; zero-based degrees `0..6`, invalid IDs/degrees, all 72 tonic/scale contexts, all twelve membership outcomes, and repeated fixed-order `nightdrive.scale.v1` serialization are validated without spelling, key, named-interval, chord, or generation semantics. |
| AC-038 | MUS-001, MUS-011, NFR-012 | All 72 numeric tonic/scale Keys construct as immutable two-field values; runtime-forged tonic or scale inputs fail; key projection, all seven degree lookups, all twelve membership results, deterministic equality, and repeated fixed-order `nightdrive.key.v1` serialization match delegated scale behavior and contain no derived arrays, spelling, or key-signature data. |
| AC-039 | MUS-012 | The documented V1 `ChordQuality` contract is one explicitly versioned vocabulary containing exactly major triad `major-triad` `[0,4,7]`, minor triad `minor-triad` `[0,3,7]`, and diminished triad `diminished-triad` `[0,3,6]`; each formula is tonic-relative, integer, unique, ascending, and membership-only. Version belongs to the vocabulary/schema contract, not redundant per-instance state, and serialization mechanics remain deferred. Root, ordering, inversions, voicings, spelling, extensions, and seventh identities are outside the quality; seventh structures are explicitly deferred extension metadata. No production ChordQuality or Chord implementation is included. |
| AC-040 | MUS-013, NFR-013 | Exactly the three canonical ChordQuality IDs construct; alternate, malformed, and forged identities fail; formulas match the approved three-value fixtures and invariants; returned formulas are immutable; all equality pairs are deterministic; repeated `nightdrive.chord-quality.v1` serialization is byte-stable and contains no root, inversion, voicing, spelling, extension, display, or per-instance version fields. |
| AC-041 | MUS-014 | The documented future Chord contract contains only root `PitchClass` and `ChordQuality` for the initial triad-only implementation; membership is derived by formula-plus-modulo-12 projection and is not stored or serialized; extensions are initially unsupported and separately versioned when authorized; equality, `nightdrive.chord.v1` boundaries, and separation from inversion, voicing, and harmony are explicit. No production Chord implementation is included. |
| AC-042 | MUS-015, NFR-014 | All 36 root/quality combinations construct as immutable two-field Chords; each derives three unique modulo-12 members without storing them; forged values fail; all 1,296 ordered equality pairs are deterministic; repeated serialization is byte-stable with exact minimal `nightdrive.chord.v1` fields and no extension, inversion, voicing, spelling, or derived fields. |
| AC-043 | MUS-016, NFR-015 | The documentation-only ChordInversion contract accepts exactly member indices `0`, `1`, and `2` in canonical formula-derived triad order; defines root/first/second inversion meaning; keeps Chord identity and voicing separate; reserves minimal `nightdrive.chord-inversion.v1` serialization; specifies malformed/forged rejection expectations; and requires explicit review before supporting wider chord cardinality. No production implementation is included. |
| AC-044 | MUS-017, NFR-016 | The ChordInversion primitive accepts exactly `0..2`, rejects malformed and forged values, passes all 9 ordered equality pairs, and produces repeated exact minimal `nightdrive.chord-inversion.v1` serialization without chord, pitch, voicing, label, or version fields. |
| AC-045 | MUS-018, NFR-017 | The documentation-only PRNG contract selects versioned Mulberry32 with an exact uint32 transition, defines seed/state/output and replay semantics, excludes ambient randomness and security use, records lineage responsibilities, defers stream/fork mechanics and standalone seed serialization to lineage/composition schema work, and specifies future deterministic known-answer and cross-runtime evidence. No PRNG implementation is included. |
| AC-046 | MUS-019, NFR-018 | The PRNG primitive accepts exactly canonical uint32 seeds, rejects malformed/coercible values, matches fixed known-answer vectors, reproduces sequences across independent executions, advances state once per call, emits uint32 values, exposes the stable identifier, and contains no ambient randomness or deferred helper/music behavior. |
| AC-047 | MUS-020, NFR-019 | The documentation-only ChordVoicing contract freezes exactly three strictly ascending unique MidiPitches, defines one-per-triad-member membership and lowest-member inversion compatibility checks, separates global range from harmony policy, reserves minimal deterministic serialization, and defers implementation, doubling, wider cardinality, and voice-leading. |
| AC-048 | MUS-021, NFR-020 | The ChordVoicing primitive accepts exactly three immutable strictly ascending MidiPitches, rejects malformed and forged values, compares ordered identity deterministically, serializes exact minimal `nightdrive.chord-voicing.v1`, and validates Chord membership and ChordInversion compatibility in canonical member order across all 12 roots, three V1 qualities, and three inversion indices without generation, harmony, wider cardinality, or new dependencies. |
| AC-049 | MUS-022, NFR-021 | Stage 4A documentation freezes a versioned harmony-template representation, concrete bounded data for all four V1 profiles, explicit degree-to-Chord and unsupported-combination behavior, inversion/voicing boundaries, integer voice-leading cost and tie-breaks, bounded PRNG participation, structured unsatisfiable reasons, provenance shape, worked examples, and implementation evidence requirements without production Harmony code or new dependencies. |
| AC-050 | MUS-023, NFR-022 | Stage 4B1 validates the complete concrete template catalog and profile/scale mappings, treats IDs as opaque, validates optional slot inversion restrictions, rejects malformed and scale-mismatched inputs, and deterministically realizes explicit zero-based degree and ChordQuality slots into ordered existing Chords with exact major, natural-minor, wrapped-root, immutability, and repeatability evidence; no voicing, voice-leading, progression, seeded variation, or Stage 4 engine behavior is implemented. |
| AC-051 | MUS-024, NFR-023 | Stage 4B2 enumerates immutable ChordInversion/ChordVoicing candidates in deterministic lexicographic order for each approved profile policy, validates range/span, Chord membership, and inversion compatibility, supports explicit inversion restrictions, rejects malformed restrictions with stable errors, and preserves wrapped-root behavior without selection, voice-leading, progression, seeded variation, or later Harmony behavior; no-voicing failure is reserved because fixed V1 policies admit candidates for every triad. |
| AC-052 | MUS-025, NFR-024 | Stage 4B3 calculates exact integer adjacent voice-leading cost and deterministically selects from an existing hard-valid target candidate set by lower cost, lower maximum pitch, lower bass, lower middle, then lower top; selection is candidate-order independent and immutable, with empty input rejected using the accepted `NO_VOICING` failure. For strictly ascending three-note voicings, lower top is redundant with lower maximum; no progression-wide optimization or later Harmony behavior is implemented. |
| AC-053 | MUS-026, NFR-025 | Stage 4B4 implements and tests one immutable sequential progression realization: validated template/Key context, explicit slot order and bars, Stage 4B2 hard-valid candidates ranked by lower Stage 4B3 adjacent cost, then applicable Stage 4B4 soft-preference rank, then the accepted Stage 4B3 pitch tie-break sequence, deterministic first-slot anchor, specific failure propagation, and non-canonical provenance, without modifying the Stage 4B3 selector, lookahead, global optimization, seeded variation, MIDI, or later Harmony behavior. |
| AC-054 | MUS-027, NFR-026 | Stage 5A documentation defines the versioned Nightdrive MIDI IR, validated canonical composition/timing input boundary, exactly 960-PPQ integer ticks, Format 1 with conductor track 0 and fixed component order/channels, explicit `0x8n` Note Off with release velocity 0, note span expansion, equal-tick ordering, exactly one End-of-Track at tick 30720 on every track, structured failures, semantic and binary determinism, and an isolated serializer adapter without implementing MIDI or adding dependencies. |
| AC-055 | NFR-026 | Stage 5A defines a bounded build-vs-buy spike comparing `midi-file` and `midi-writer-js` against deterministic fixtures, independent parsing, runtime support, malformed behavior, license/health/security, size/transitives, and replaceability; a candidate may be adopted only when evidence preserves the Nightdrive IR behind the adapter, otherwise custom encoding requires architecture review. |

## Release interpretation

Automated musical validity is necessary but does not prove quality. Version 1 additionally requires structured human review of representative golden cases and the FL Studio import workflow. A criterion may be “not yet implemented” until its roadmap stage is authorized; documentation alone does not satisfy product behavior.
